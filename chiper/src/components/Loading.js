import React from "react";

const Loading = () => {
    return (
        <div className="contentLoading">
            <div className="spinner"></div>
        </div>
    );
};

export default Loading;
